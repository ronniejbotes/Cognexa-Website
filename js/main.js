// Cognexa Website — shared site scripts.
// Progressive enhancement only: pages must remain fully readable without JS.

document.addEventListener('DOMContentLoaded', () => {
  // Placeholder for site-wide behavior (mobile nav toggle, scroll effects, etc.)
});
